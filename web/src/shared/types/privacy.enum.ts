export enum PrivacyType {
  PUBLIC = 'public',
  PRIVATE = 'private',
}

export enum PrivacyNetworkType {
  ANYONE_CAN = 'anyoneCan',
  INVITE_ONLY = 'inviteOnly',
  INVITE_ONLY_BY_ENDORSED = 'inviteOnlyByEndorsed',
  INVITE_ONLY_BY_ADMIN = 'inviteOnlyByAdmin',
}